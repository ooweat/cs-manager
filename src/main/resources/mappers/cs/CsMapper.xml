<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.company.cs.mappers.CsMapper">
  <select id="findAllTermialModel" resultType="kr.co.company.cs.domain.SeqName">
    SELECT TID_SEQ AS SEQ,
           TID_MODEL_NAME AS NAME
    FROM TB_TERMINAL_MODEL
    ORDER BY TID_SEQ ASC
  </select>
  <select id="findAllByRevisionType" resultType="kr.co.company.cs.domain.SeqName">
    SELECT SEQ AS SEQ, NAME AS NAME, R_TYPE AS TYPE
    FROM TB_REVISION
    ORDER BY SEQ ASC
  </select>
</mapper>
